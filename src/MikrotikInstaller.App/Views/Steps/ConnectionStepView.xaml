<UserControl x:Class="MikrotikInstaller.App.Views.Steps.ConnectionStepView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignWidth="600">
    <StackPanel Margin="0,16,0,0">
        <TextBlock Text="{Binding Description}" TextWrapping="Wrap" Margin="0,0,0,24" Opacity="0.8" />

        <TextBlock Text="Adresse des Geräts (IP oder Hostname)" Margin="0,0,0,4" />
        <ui:TextBox PlaceholderText="z. B. 192.168.88.1" Margin="0,0,0,16" />

        <TextBlock Text="Benutzername" Margin="0,0,0,4" />
        <ui:TextBox PlaceholderText="admin" Margin="0,0,0,16" />

        <TextBlock Text="Passwort" Margin="0,0,0,4" />
        <ui:PasswordBox Margin="0,0,0,16" />

        <ui:Button Content="Verbindung testen" Appearance="Secondary" HorizontalAlignment="Left" />
    </StackPanel>
</UserControl>
