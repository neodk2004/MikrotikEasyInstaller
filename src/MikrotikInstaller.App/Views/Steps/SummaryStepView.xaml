<UserControl x:Class="MikrotikInstaller.App.Views.Steps.SummaryStepView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignWidth="600">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="0,16,0,0">
            <TextBlock Text="{Binding Description}" TextWrapping="Wrap" Margin="0,0,0,20" Opacity="0.8" />

            <StackPanel Visibility="{Binding IsFinished, Converter={StaticResource BoolToVisibilityConverter}}" Margin="0,0,0,20">
                <TextBlock Text="Fertig! Dein Gerät wurde erfolgreich eingerichtet." FontWeight="SemiBold" Foreground="Green" Margin="0,0,0,8" />
                <TextBlock TextWrapping="Wrap">
                    <Run Text="Ein Backup deines vorherigen Zustands liegt auf dem Gerät unter dem Namen „" />
                    <Run Text="{Binding BackupName}" FontWeight="SemiBold" />
                    <Run Text="“." />
                </TextBlock>
            </StackPanel>

            <TextBlock Text="Geplante Änderungen" FontWeight="SemiBold" Margin="0,0,0,8"
                       Visibility="{Binding IsFinished, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=Invert}" />

            <ItemsControl ItemsSource="{Binding PlannedChanges}" Margin="0,0,0,20">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" TextWrapping="Wrap" Margin="0,0,0,6" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <ui:Button Content="Jetzt einrichten" Appearance="Primary" HorizontalAlignment="Left"
                       Command="{Binding ApplyCommand}"
                       Visibility="{Binding IsFinished, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=Invert}" />

            <StackPanel Orientation="Horizontal" Margin="0,16,0,0"
                        Visibility="{Binding IsApplying, Converter={StaticResource BoolToVisibilityConverter}}">
                <ui:ProgressRing IsIndeterminate="True" Width="20" Height="20" Margin="0,0,12,0" />
                <TextBlock Text="{Binding ProgressText}" VerticalAlignment="Center" TextWrapping="Wrap" />
            </StackPanel>

            <TextBlock Text="{Binding ErrorMessage}" TextWrapping="Wrap" Foreground="Red" Margin="0,16,0,0"
                       Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}" />
        </StackPanel>
    </ScrollViewer>
</UserControl>
