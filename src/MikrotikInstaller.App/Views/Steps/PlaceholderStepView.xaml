<UserControl x:Class="MikrotikInstaller.App.Views.Steps.PlaceholderStepView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignWidth="600">
    <StackPanel Margin="0,16,0,0">
        <TextBlock Text="{Binding Description}" TextWrapping="Wrap" Opacity="0.8" />
        <TextBlock Text="(Dieser Schritt wird in einer späteren Ausbaustufe mit Inhalten gefüllt.)"
                   Margin="0,16,0,0" FontStyle="Italic" Opacity="0.5" />
    </StackPanel>
</UserControl>
