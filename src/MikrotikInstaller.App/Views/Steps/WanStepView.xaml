<UserControl x:Class="MikrotikInstaller.App.Views.Steps.WanStepView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignWidth="600">
    <StackPanel Margin="0,16,0,0">
        <TextBlock Text="{Binding Description}" TextWrapping="Wrap" Margin="0,0,0,24" Opacity="0.8" />

        <TextBlock Text="Internet-Schnittstelle" Margin="0,0,0,4" />
        <ComboBox ItemsSource="{Binding AvailableInterfaces}" SelectedItem="{Binding SelectedInterface}"
                  Margin="0,0,0,20" HorizontalAlignment="Left" MinWidth="220" />

        <CheckBox Content="Feste IP-Adresse statt automatischem Bezug (DHCP) verwenden"
                  IsChecked="{Binding UseStaticAddress}" Margin="0,0,0,16" />

        <StackPanel Visibility="{Binding UseStaticAddress, Converter={StaticResource BoolToVisibilityConverter}}">
            <TextBlock Text="Feste IP-Adresse (mit Präfix, z. B. 203.0.113.5/24)" Margin="0,0,0,4" />
            <ui:TextBox Text="{Binding StaticAddress, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,16" />

            <TextBlock Text="Gateway (Adresse deines Internetanbieters)" Margin="0,0,0,4" />
            <ui:TextBox Text="{Binding StaticGateway, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,16" />
        </StackPanel>
    </StackPanel>
</UserControl>
