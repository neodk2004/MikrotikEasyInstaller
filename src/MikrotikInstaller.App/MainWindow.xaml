<ui:FluentWindow x:Class="MikrotikInstaller.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MikrotikInstaller.App"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:vm="clr-namespace:MikrotikInstaller.App.ViewModels"
        mc:Ignorable="d"
        Title="MikroTik Einrichtungs-Assistent" Height="560" Width="820"
        WindowStartupLocation="CenterScreen"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica">

    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>

    <Grid Margin="0,0,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="MikroTik Einrichtungs-Assistent" />

        <StackPanel Grid.Row="1" Margin="32,16,32,0">
            <TextBlock Text="{Binding CurrentStep.Title}" FontSize="24" FontWeight="SemiBold" />
            <TextBlock Text="{Binding ProgressText}" Opacity="0.6" Margin="0,4,0,0" />
            <ProgressBar Minimum="1" Maximum="{Binding Steps.Count}" Value="{Binding CurrentStepIndex, Mode=OneWay}"
                         Height="4" Margin="0,12,0,0" />
        </StackPanel>

        <ContentControl Grid.Row="2" Content="{Binding CurrentStep}" Margin="32,16,32,16" />

        <Grid Grid.Row="3" Margin="32,0,32,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <ui:Button Grid.Column="1" Content="Zurück" Command="{Binding GoBackCommand}" Margin="0,0,8,0" />
            <ui:Button Grid.Column="2" Content="Weiter" Appearance="Primary" Command="{Binding GoNextCommand}" />
        </Grid>
    </Grid>
</ui:FluentWindow>
